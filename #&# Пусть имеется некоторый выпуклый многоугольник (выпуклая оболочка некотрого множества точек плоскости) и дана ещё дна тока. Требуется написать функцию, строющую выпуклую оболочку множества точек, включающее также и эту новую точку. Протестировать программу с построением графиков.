Перестановка двух элементов (4)
function swap!(array::AbstractVector, i::Int, j::Int)
    temp = array[i]
    array[i] = array[j]
    array[j] = temp
end

function swap!(array, i, j)
    temp = array[i]
    array[i] = array[j]
    array[j] = temp
end
